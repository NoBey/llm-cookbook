# 原理、接口、思考：大模型到智能体的实践记录

---

## 引





---

## 第一章：LLM 的原理、训练、工程

### 1.1 模型的本质

**下一个 token 的预测器。**

给定一段文本，模型预测下一个最可能出现的词。不断重复，就是你看到的"回复"。

本质上是统计规律的拟合，但足够大的数据 + 足够深的网络，涌现出了"理解"和"推理"的能力。

### 1.2 如何让模型具有智能

**预训练**：在 TB 级别的人类文本上，用下一个 token 预测任务训练。模型从中学到语言结构、世界知识、逻辑关系。

**注意力机制（Attention）**：让模型在处理每个 token 时，能"回头看"上下文中任意位置的信息，而不是只看固定窗口。这是 Transformer 的核心，也是大模型能处理长上下文的原因。

```
Q · K^T / √d → softmax → V
```

每个 token 对上下文中其他 token 的"关注程度"是动态计算的，不是固定的。

### 1.3 如何让模型学会对话

**SFT（监督微调）**：用人工标注的"问题-回答"对继续训练预训练模型。

预训练模型只会"续写"，SFT 让它学会"指令跟随"——把 prompt 理解为任务，而不是上文的延续。

数据质量 > 数据数量。几千条高质量对话能显著改变模型行为。

### 1.4 如何让模型学会推理

单靠 SFT 不够，推理能力需要 RL（强化学习）后训练：

- **奖励模型（Reward Model）**：学习"什么是好回答"，为每条输出打分
- **PPO / GRPO**：用奖励信号更新策略模型，让高分回答的概率上升
- **过程奖励 vs 结果奖励**：奖励每一步推理 vs 只奖励最终答案，前者更难但效果更好

> 预训练阶段各家差距已不大。现阶段模型的 agent 能力差距，主要来自后训练阶段的 RL 数据质量和训练策略。

### 1.5 如何把模型部署到生产

**ChatML 格式**：将多轮对话结构化为 messages 数组，每条消息带 `role` 和 `content`，模型输出时生成 `assistant` 角色的回复。

```
<|im_start|>system
你是一个助手
<|im_end|>
<|im_start|>user
今天天气怎么样？
<|im_end|>
<|im_start|>assistant
```

API 层将这个格式序列化为 JSON，流式输出时通过 SSE 逐 token 推送。

**推理服务**：
- **ollama**：本地部署，开发调试用
- **vllm**：生产级高吞吐推理，支持连续批处理（Continuous Batching）

**KV 缓存**：
- Attention 计算中 K、V 矩阵可以缓存复用，避免重复计算相同的上下文前缀。
- 模型只做下一个 token 预测

**think 强度**
- 本质是拼接 提示词

---

## 第二章：LLM API、Agent、工具链

### 2.1 LLM API 格式

**OpenAI 格式**已成为事实标准，几乎所有推理服务都兼容：

```json
{
  "model": "gpt-4o",
  "messages": [
    { "role": "system", "content": "你是一个助手" },
    { "role": "user", "content": "北京天气怎么样？" }
  ],
  "tools": [...],
  "stream": true
}
```

**role 优先级**：`system` > `user` > `assistant`。模型通常更信任 system 层的指令，但这不是绝对的——强烈的 user 指令可以覆盖 system 的软约束。


### 2.2 Loop、Agent、Harness

**ReAct 模式**：Think → Act → Observe，循环直到任务完成。

```
用户输入
  ↓
LLM 推理（带 tools 列表）
  ↓  finish_reason = "tool_calls"
执行 tool，获取结果
  ↓  追加 role: "tool" 消息
LLM 继续推理
  ↓  finish_reason = "stop"
最终回复
```

**Harness** 是这个 loop 的调度层：管理 messages 历史、分发 tool calls、处理并发、控制最大轮次防止死循环。

> 单次 LLM 调用是无状态的，agent 的"状态"完全存在于不断追加的 messages 里。

### 2.3 Skill 实现原理

通过 LLM API 的 `tools` 字段声明能力，LLM 决定"调不调"和"调哪个"，宿主程序负责"真正执行"。

**Skill 契约**：每个 skill 导出 `definition`（告诉 LLM 能做什么）和 `execute`（实际执行）：

```js
// skills/get_weather.js
export const definition = {
  type: "function",
  function: {
    name: "get_weather",
    description: "获取指定城市的当前天气",
    parameters: {
      type: "object",
      properties: { city: { type: "string" } },
      required: ["city"],
    },
  },
};

export async function execute({ city }) {
  return JSON.stringify({ city, temperature: "22°C", condition: "晴" });
}
```

**并行 tool calls**：单轮 LLM 输出可以包含多个 tool_calls，应并行执行，全部完成后再一起回填，不要串行等待。

```js
const results = await Promise.all(
  toolCalls.map(tc => skillLoader.dispatch(tc))
);
```

---

## 第三章：用 LLM 的思维方式思考

### 3.1 出现问题：从 Agent 层到模型层逐层排查

**例子**：Cursor 用中文提问，模型却用英文回复。

- **Agent 层排查**：拦截实际发出的 API 请求，检查 messages 里有没有英文指令、英文示例把模型"带偏"了
- **模型层排查**：看 `thinking` 内容——如果推理过程是英文，说明 RL 阶段的训练数据以英文为主，模型"英文思考"更顺畅，进而输出英文

排查顺序：**先看输入，再看推理，最后才是模型本身的问题。**

### 3.2 把 LLM 调用理解为纯函数

```
output = LLM(context)
```

LLM 是无副作用的——它只"计算"，不"执行"。所有真实的副作用（写文件、发请求、改数据库）都发生在 tool 的 `execute` 里。

```
模型层：context += LLM(context)
应用层：context += LLM(context) + execute(tool_calls)
```

这个视角很有用：调试时可以固定 context，重复调用验证输出；上线时可以在 execute 层加鉴权、日志、限流，LLM 层完全不用改。

### 3.3 逻辑 vs 知识

**逻辑通常不会错，知识可能是错的。**

洗车问题：让模型帮你规划"去洗车"，它可能建议你推着车去，或者走路去。这不是推理出了问题，而是模型缺少"洗车需要开车去"这个常识——因为这个知识在训练数据里不够显著。

模型的知识来自训练语料，而语料对真实世界的覆盖是不均匀的。遇到"奇怪的错误"，先检查是不是知识缺失，而不是质疑模型的推理能力。

**应对策略**：把关键的领域知识通过 RAG 或 system prompt 显式注入，不要依赖模型的"记忆"。

### 3.4 LLM 的输出是创新吗？

本质上是**已有知识的插值和重组**，不是凭空创造。

"写一首从没人写过的诗"——模型能写出来，但它依赖的是对已有诗歌风格、意象、结构的拟合，而不是真正意义上的创作突破。

实际意义：**好的输入是好输出的前提。** 提示词的质量、上下文的丰富程度，直接决定输出天花板。

---

## 第四章：实践技巧

### 4.1 控制 tools 数量

tools 列表过长，模型的"选择困难"会上升，调用准确率下降，延迟增加（tool definitions 占 token）。

**原则**：每次只注册当前任务相关的 tools，不要把所有 skills 全部挂载。动态注册比静态挂载更灵活。

### 4.2 控制上下文长度

context 越长：推理越慢、成本越高、模型对早期内容的"注意力"越稀薄（Lost in the Middle 问题）。

**策略**：
- 长对话做滚动摘要，替换掉早期的原始消息
- tool 返回结果做精简，只保留 LLM 下一步需要的字段
- 善用 KV 缓存：固定 system prompt，减少重复计算

### 4.3 事实验证（API 调用尤其注意）

模型对 API 的知识有截止日期，且可能张冠李戴。让模型生成 API 调用代码时，**必须对照文档核实对应版本中该接口是否存在、参数是否正确**。

不要让模型"自信地"调一个不存在的接口，这是 agent 里最常见的无效循环来源。

### 4.4 语音 + 文本混合输入

实践中发现：**不需要严谨，但需要完整。**

语音输入口语化、有歧义没关系，但信息要完整——背景、目标、约束都说清楚。模型有很强的意图识别能力，但它无法凭空补全你没说的约束条件。

### 4.5 不要让模型做关键决策

模型可以"建议"，但**执行前要有人（或规则系统）确认**。

特别是不可逆操作：删除文件、发送消息、提交代码、扣款。把这类 tool 设计成"需要二次确认"，而不是 LLM 一次推理直接执行完毕。

> agent 出问题，往往不是模型不够聪明，而是给了它太大的权限和太少的约束。

---

## 总结

| 层次 | 核心概念 | 关键认知 |
|------|----------|----------|
| 模型层 | token 预测 + RL 后训练 | 预训练给知识，RL 给推理能力 |
| API 层 | ChatML + tool calling | 格式标准化，LLM 只计算不执行 |
| Agent 层 | loop + harness | 状态在 messages 里，副作用在 tool 里 |
| 思维层 | 纯函数视角 | 调试从输入查起，知识错误 ≠ 逻辑错误 |
| 工程层 | 上下文 + tools 管理 | 少即是多，关键决策要人守门 |

从 LLM 到 Agent，技术栈不复杂，但认知要先到位。理解了模型在"做什么"，才能知道工程上该"控制什么"。
